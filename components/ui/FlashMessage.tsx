import React from 'react';
import { View } from 'react-native';

export const FlashMessage: React.FC = () => {
  // Placeholder para flash messages
  // Em uma implementação real, você usaria react-native-flash-message
  return <View />;
};
